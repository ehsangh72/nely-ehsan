{
  "name": "edge-gateway-proxy",
  "version": "2.0.0",
  "description": "Advanced edge proxy service",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Build complete'",
    "deploy": "netlify deploy --prod"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}